name: Deploy Blazemark Blog

on:
  push:
    branches:
      - main   # 当 main 分支有更新时触发

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      # 3. 安装依赖（如果有 requirements.txt）
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "No requirements.txt found"

      # 4. 构建 Blazemark 静态站点
      - name: Build site
        run: |
          python blazemark.py build --force

      # 5. 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GH_TOKEN }}   # 或者使用 Personal Access Token
          publish_dir: ./public
          publish_branch: main     # 或 gh-pages，根据你的仓库设置
          user_name: "bcsm114514"
          user_email: "3157296825@qq.com"
